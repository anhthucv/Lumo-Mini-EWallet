export interface AdminTransaction { transactionId: number; createdAt: string; type: string; status: string; amount: number | string; balanceAfterTransaction: number | string; journalId: number | null; userId: number | null; userEmail: string | null; userFullName: string | null; accountId: number; maskedAccountNumber: string | null; relatedAccountId: number | null; }
export interface AdminLedgerEntry { entryId: number; ledgerAccountId: number; ledgerAccountCode: string; ledgerAccountType: string; direction: string; amount: number | string; createdAt: string; }
export interface AdminTransactionDetail extends AdminTransaction { ledgerEntries: AdminLedgerEntry[]; journalReference: string | null; journalCreatedAt: string | null; topUp: { topUpId: number; provider: string | null; status: string | null; providerReference: string | null; merchantOrderCode: number | null; amount: number | string; } | null; }
export interface AdminDashboard { users: { total: number; active: number; locked: number }; wallets: { total: number }; transactions: { total: number; successful: number; depositVolume: number | string; transferVolume: number | string }; recentActivity: AdminTransaction[]; }
